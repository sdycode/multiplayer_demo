


    // messageStream = ref.child(owner.uid).onValue.map((event) {

      //  final ref = FirebaseDatabase.instance
      //   .ref()
      //   .child(appanmeForFirebase)
      //   .child("bookings_ids")
      //   .child(auth.currentUser!.uid);



  //      final ref =
  //     FirebaseDatabase.instance.ref().child(appanmeForFirebase).child("ids");
  // DatabaseEvent dbevent = await ref.once();
  // // dblog("dbevent ids ${dbevent.snapshot.children.length}");
  // for (DataSnapshot e in dbevent.snapshot.children) {
  //   Map map = e.value as Map;